import { DefaultGeneratorOptions } from '@nx-extend/core';
import { Tree } from '@nx/devkit';
import { Provider } from '../../utils/provider';
export interface InitOptions extends DefaultGeneratorOptions {
    provider?: Provider;
    secretsProvider?: string;
    login?: string;
}
export default function (tree: Tree, rawOptions: InitOptions): Promise<void>;
