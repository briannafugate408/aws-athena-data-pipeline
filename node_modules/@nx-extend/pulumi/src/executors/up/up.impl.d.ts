import { ExecutorContext } from '@nx/devkit';
export interface UpOptions {
    stack?: string;
    skipPreview?: boolean;
    yes?: boolean;
    suppressOutputs?: boolean;
    json?: boolean;
    root?: string;
}
export default function createExecutor(options: UpOptions, context: ExecutorContext): Promise<{
    success: boolean;
}>;
