import { ExecutorContext } from '@nx/devkit';
export interface RefreshOptions {
    stack?: string;
    skipPreview?: boolean;
    yes?: boolean;
    root?: string;
}
export default function createExecutor(options: RefreshOptions, context: ExecutorContext): Promise<{
    success: boolean;
}>;
