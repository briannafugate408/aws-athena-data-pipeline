import { ExecutorContext } from '@nx/devkit';
export interface PreviewOptions {
    stack?: string;
    root?: string;
}
export default function creatExecutor(options: PreviewOptions, context: ExecutorContext): Promise<{
    success: boolean;
}>;
